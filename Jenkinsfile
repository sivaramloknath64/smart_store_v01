pipeline {
	agent any
	stages {
		stage('nugget restore'){
			steps{
				  echo "nugget restoring packages"
				
				
				
					}
				}
				stage('Build') {
    					steps {
              echo   " building "
    					}
				}
			}
}
